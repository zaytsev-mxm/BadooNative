import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    root: {
        borderColor: '#a0a0a0',
        borderWidth: 2,
        height: 40,
        width: '90%',
        marginBottom: 12,
        borderRadius: 8,
        paddingLeft: 12,
        paddingRight: 12,
    },
    input: {
        flex: 1,
    },
});
